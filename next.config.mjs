/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false,

    images: {
    domains: ['r2.flowith.net','r2.icool-test.xin'],
  },
};

export default nextConfig;
